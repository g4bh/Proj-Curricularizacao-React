import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    botao: {
    backgroundColor: '#2c3e50',
    padding: 15,
    borderRadius: 12,
    marginTop: 20,
    alignItems: 'center',
  }
});

export {styles}