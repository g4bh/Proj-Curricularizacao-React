import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    input: {
    backgroundColor: '#fff',
    borderRadius: 12,
    padding: 30,
    marginBottom: 10,
    fontSize: 16,
  }
});

export {styles}